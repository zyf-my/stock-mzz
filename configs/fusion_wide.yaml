# hist_wide 若 valid > 0.104，再与截面基线融合。
include: configs/default.yaml

inputs:
  temporal_valid: outputs/hist_wide_valid.npy
  temporal_test: outputs/hist_wide_test.npy
  cross_section_valid: outputs/baseline_valid.npy
  cross_section_test: outputs/baseline_test.npy
  industry_col: 6

fusion:
  weight_grid: [0.0, 0.25, 0.5, 0.7, 0.85, 1.0]

paths:
  valid_pred: outputs/fusion_wide_valid.npy
  test_pred: outputs/fusion_wide_test.npy
  submission: submissions/task1_fusion_wide.npy
