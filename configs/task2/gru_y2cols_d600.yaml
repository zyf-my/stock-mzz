# y2 Top21 有限截面列 + 末 600 天 + 含当天 + n2000。
# 不覆盖 gru_y2cols / x6_d600 / 主融合。

include: configs/task2/gru_y2cols.yaml

features:
  recent_days: 600
  max_train_stocks_per_day: 2000

train:
  recent_days: 600

paths:
  checkpoint: checkpoints/task2/gru_y2cols_d600.pt
  valid_pred: outputs/task2/gru_y2cols_d600_valid.npy
  test_pred: outputs/task2/gru_y2cols_d600_test.npy
  submission: submissions/task2_gru_y2cols_d600.npy
