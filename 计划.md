# 靖戈企业命题一 · 工程计划

本文档是本仓库的工作说明书。任何人改代码、加实验、准备提交前，先读这里，并在对应条目上改状态。

- 状态标记：`未开始` / `进行中` / `已完成` / `阻塞`
- 改计划时：只改事实、任务和验收标准，不要删掉已完成项；用删除线或「已完成」标记即可
- 赛题截止日期以组委会最新通知为准，本文件不写死日历，按阶段推进

当前总状态：**阶段 1 已完成，阶段 2 未开始**

---

## 1. 赛题是什么

赛事：2026 友安杯南京大学「人工智能+」大学生创新技能挑战赛  
赛道：靖戈企业命题 · 时间序列预测 1（课题 1，标签 `y1`）

任务：用股票市场面板数据，预测每只股票在每个交易日的「潜力」分数。评价的不是单点涨跌幅误差，而是**每个交易日横截面上的排序质量**。

官方指标：

1. 在股票维 `S` 上，对预测与标签计算 RankIC（Spearman 等级相关）
2. 得到长度为 `T` 的向量
3. 取均值，越大越好
4. 官方门槛：测试集 mean RankIC **不低于 0.12**

官方提示（必须体现在方法和文档里）：

1. 在时间维 `T` 上用时序模型，效果通常更好
2. 也可以在股票维 `S` 上建模，并与时序结果融合

提交入口：<https://satc.jingle.ai/submit>（需先注册 <https://satc.jingle.ai/>）

课题 2（标签 `y2`）与课题 1 共用同一份数据文件。本仓库当前只做课题 1。不要用 `y2` 当 `y1` 的特征，除非单独开实验并写进实验记录（存在泄漏风险）。

---

## 2. 评分构成（做东西时按这个分配精力）

| 维度 | 权重 | 评委看什么 | 本仓库对应动作 |
|---|---|---|---|
| 结果质量 | 40% | 测试集 RankIC，门槛 0.12 | 本地验证集对齐官方公式；测试集只用于最终提交，禁止拿来调参 |
| 设计质量 | 20% | 代码结构、文档、可复现 | 配置驱动；数据 / 训练 / 提交分离；实验有记录 |
| 体验性 | 20% | 流程是否顺、训练是否过慢 | 先保可跑通的树模型流水线，深度模型控制窗口和参数量，文档写清耗时和硬件 |
| 创新性 | 20% | 是否针对该场景 | 按官方提示做时间维 + 股票维融合，而不是只换骨干网络名字 |

决策原则：

- 没有超过最强单因子的可复现基线之前，不上重模型
- 验证集 RankIC 的提升，必须能用消融说明来源（特征、损失、时序、融合）
- 训练过慢会丢体验分。深度模型若只比树模型高一点、但慢一个数量级，默认不作为主提交，可作融合的一支

---

## 3. 硬约束（违反即视为任务失败）

### 3.1 数据保密

依据：《参赛团队数据资料保密承诺书》

- 赛题数据仅用于本届指定赛题，不得用于商业、论文发表、第三方开发
- 未经组委会与赛题单位书面授权，不得向其他队伍、同学、公司泄露数据、技术细节或使用情况
- 数据禁止提交进 git，禁止上传公开网盘、公共服务器、个人云同步目录（OneDrive、QQ 同步空间等）
- 本机存放需权限控制；赛后 10 个工作日内删除全部数据资料
- 成果展示或论文如需用到数据，必须先向赛题单位书面申请并脱敏

仓库侧已做：`.gitignore` 忽略 `data/`、`outputs/`、`checkpoints/`、`submissions/`、`*.npy`、`*.npz`、`*.pkl`、`*.pickle`、`*.zstd`、`*.zst`

任何人执行 `git add` 前必须确认暂存区没有数据包和解包后的 pickle。

### 3.2 本机数据位置（不要拷进仓库）

当前开发机：

| 文件 | 路径 | 大小 | 说明 |
|---|---|---|---|
| 官方压缩包 | `D:\contest-data-private\data.z` | 3.03 GiB | pickle 包一层的 zstd 字节流 |
| 解包后的 dict | `D:\contest-data-private\data_unpacked.pkl` | 8.47 GiB | 可直接 `pickle.load`，后续脚本优先读这个 |

其他机器：自行放到仓库外的受控目录，用环境变量或配置文件传入路径，例如 `JINGGE_DATA=...`。不要把绝对路径写死进多处代码，只允许出现在本地配置。

读取方式与官方一致：外层 pickle → zstd 解压 → 内层 pickle 得到 `dict`。`src/io.py` 已兼容「直接读解包 dict」。

### 3.3 禁止信息泄漏

- 只按时间切分，禁止按股票或按样本随机打乱后划分 train/valid/test
- 特征窗口、归一化、类别编码、缺失填充的统计量，只能在训练集（或当前时间点之前）上拟合
- 验证集、测试集标签不得进入训练损失，测试集 `y1` 本身就是空的
- `y2` 默认不可用作 `y1` 的输入
- 股票 ID 类高基数类别（见 4.4）不得直接当普通数值特征乱拟合到测试集未见 ID 上而不做说明

### 3.4 提交格式（平台拒收 = 本阶段未完成）

- 类型：`np.ndarray`，`dtype=np.float32`
- 形状：`(Tt, S)`，其中 `Tt = T - test_start_idx`。当前数据下为 **`(442, 5282)`**
- 文件：`np.load(path)` **不带** `allow_pickle` 等额外参数即可得到该数组
- 使用 `src/submit.py` 的 `save_submission` 保存，禁止手写一种对不上的格式
- 测试日没有标签的股票也必须在 `S` 维占位；无效位置可填 0 或截面均值，但形状不能缺列

---

## 4. 数据事实（阶段 1 已核实，改模型前不要推翻）

来源：对 `data_unpacked.pkl` 的探查。复现命令：

```text
.\.venv\Scripts\python.exe scripts\inspect_data.py --file D:\contest-data-private\data_unpacked.pkl
```

全量 99 列 RankIC 扫描已跑过一次（约 8 分钟）。不要无故再全量扫描；若重跑，把结果写进 `outputs/eda/`（该目录 gitignore）。

### 4.1 字段

| 键 | 含义 | dtype | 形状 |
|---|---|---|---|
| `num_x` | 数值特征 | float32 | `(3603, 5282, 99)` |
| `cat_x` | 类别特征 | int64 | `(3603, 5282, 9)` |
| `y1` | 课题 1 标签 | float32 | `(3603, 5282)` |
| `y2` | 课题 2 标签 | float32 | `(3603, 5282)` |
| `mask_x` | 特征有效位，`False` 表示无信息 | bool | `(3603, 5282)` |
| `mask_y` | 标签有效位，`False` 表示无信息 | bool | `(3603, 5282)` |
| `train_start_idx` | 训练集起点 | int | 标量，值为 **486** |
| `valid_start_idx` | 验证集起点 | int | 标量，值为 **2918** |
| `test_start_idx` | 测试集起点 | int | 标量，值为 **3161** |

`num_x` 在已检查范围内无 NaN、无 Inf。`mask_x=True` 的位置特征齐全。

### 4.2 时间切分

| 集合 | 切片 | 天数 | 标签 |
|---|---|---|---|
| 历史缓冲 | `[0 : 486]` | 486 | 有特征，**不算进训练集**。时序窗口可以回看这些天 |
| train | `[486 : 2918]` | 2432 | `y1` 可见 |
| valid | `[2918 : 3161]` | 243 | `y1` 可见，本地唯一调参依据 |
| test | `[3161 : 3603]` | 442 | `y1` 全空，只能上传后看分 |

验证集只有 243 天，单次 RankIC 会有波动。比较模型时看验证集分数，不要为 0.005 量级抖动反复改参。最终选模型用「验证集分数 + 训练耗时 + 复杂度」，不要用测试集回灌。

### 4.3 标签与缺失

- `y1` 有效值落在 `[0, 1]`，训练集/验证集在 `mask_y=True` 上均值均为 0.50，标准差约 0.289，很接近均匀分布。按天看也是均值 0.50、最小约 0、最大约 1。应按**截面排序/分位数**处理，而不是当原始收益率回归
- 测试集 `y1` 全部不可用（NaN）
- `y2` 形态与 `y1` 类似；训练集上 `y1` 与 `y2` 的 RankIC 约 **0.42**，相关但不是同一任务
- `mask_x` 为 False 的比例约 39.42%；`mask_y` 为 False 约 46.20%
- 训练集按日：`mask_x` 有效股票数约 2085–4239（中位 2873）；`mask_y` 约 1208–3815（中位 2460）
- `mask_y=True` 的位置，`mask_x` 几乎总是 True（探查时「有标签无特征」比例为 0）。损失和 RankIC 以 `mask_y` 为准即可，推理时无特征的位置不要当有效样本

RankIC 计算规则（已实现于 `src/metrics.py`）：

- 输入 `pred`、`label` 形状均为 `(T, S)`
- 每个交易日 `t`，只在 `mask_y[t]=True` 且预测/标签有限的股票上算 Spearman
- 有效股票少于 3 只，或名次退化为常数时，记为 NaN，最后对时间取 `nanmean`
- 训练、验证、对比实验必须调用同一套函数，禁止另写一套相关计算

### 4.4 类别特征（训练集、`mask_x=True`）

| 列 | 唯一值个数 | 备注 |
|---|---|---|
| `cat_x[..., 0]` | 6 | 低基数 |
| `cat_x[..., 1]` | 13 | 低基数 |
| `cat_x[..., 2]` | 3 | 低基数 |
| `cat_x[..., 3]` | 3 | 低基数 |
| `cat_x[..., 4]` | 3 | 低基数 |
| `cat_x[..., 5]` | 约 4094 | 高基数，疑似股票 ID。树模型可当类别，神经网络不要直接巨大 embedding 除非有方案 |
| `cat_x[..., 6]` | 33 | 中基数，疑似行业 |
| `cat_x[..., 7]` | 3 | 低基数 |
| `cat_x[..., 8]` | 5 | 低基数 |

阶段 2 默认：低/中基数类别进模型；`cat_x[..., 5]` 单独决定是否使用，用了要在实验记录里写原因（过拟合风险）。

### 4.5 单因子强度（训练集 mean RankIC）

最强单列是 `num_x[..., 8]`，训练集 RankIC **−0.079187**（方向为负，模型可学翻转）。同一列验证集约 **−0.0869**。

绝对值 Top 15（训练集）：

| 列 | RankIC |
|---|---|
| 8 | −0.079187 |
| 11 | −0.066130 |
| 7 | −0.064739 |
| 41 | −0.061096 |
| 57 | +0.059713 |
| 90 | −0.055448 |
| 68 | −0.055005 |
| 40 | −0.051159 |
| 69 | −0.050494 |
| 73 | −0.046746 |
| 74 | −0.046660 |
| 39 | −0.044880 |
| 42 | −0.044364 |
| 66 | −0.042417 |
| 58 | −0.041931 |

汇总：最大绝对值 0.079；28 列绝对值 > 0.03；9 列绝对值 > 0.05。

**含义：** 官方门槛 0.12 高于任何单列。阶段 2 的第一验收标准是：组合模型验证集 RankIC 的绝对值稳定高于 0.079。若做不到，先修样本构造和 mask，不要上神经网络。

---

## 5. 仓库现状

已有：

| 路径 | 职责 | 状态 |
|---|---|---|
| `src/io.py` | 读官方数据 / 解包 pickle，给出 train/valid/test 切片 | 可用 |
| `src/config.py` | 读 yaml、解析数据路径 | 可用 |
| `src/dataset.py` | 按 split 切片、按日迭代；摊平样本函数待阶段 2 实现 | 骨架 |
| `src/metrics.py` | RankIC 序列与均值 | 可用，全量扫描较慢，后续可向量化，但公式不要改 |
| `src/submit.py` | 保存并回读提交文件 | 可用 |
| `src/models/` | `baseline` / `temporal` / `fusion` | 接口已留，训练未实现 |
| `scripts/inspect_data.py` | 打印形状、切分、缺失、前 8 列 RankIC | 可用 |
| `scripts/train_baseline.py` | 阶段 2 入口 | 骨架，调用后会提示未实现 |
| `scripts/eval_rankic.py` | 对已有预测算 RankIC | 可用 |
| `scripts/export_submission.py` | 检查并写出提交 npy | 可用 |
| `configs/` | `default` / `baseline` / `temporal` / `fusion` | 可改，数据路径用环境变量 `JINGGE_DATA` |
| `experiments.md` | 实验记录 | 只有模板 |
| `outputs/` `checkpoints/` `submissions/` `logs/` | 产物目录 | 仅 `.gitkeep` 入库 |
| `requirements.txt` | 依赖清单 | 已含 lightgbm / torch |
| `.venv/` | 本地虚拟环境 | gitignore，勿提交 |
| `README.md` | 仅两行占位 | 阶段 6 重写 |

目录（不要另起一套）：

```text
stock-mzz/
  计划.md
  experiments.md
  README.md
  requirements.txt
  configs/
    default.yaml
    baseline.yaml
    temporal.yaml
    fusion.yaml
  src/
    io.py
    config.py
    dataset.py
    metrics.py
    submit.py
    models/
      baseline.py
      temporal.py
      fusion.py
  scripts/
    inspect_data.py
    train_baseline.py
    eval_rankic.py
    export_submission.py
  outputs/
  checkpoints/
  submissions/
  logs/
```

Python 入口统一用仓库内 `.venv`：

```text
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

内存：整包解包约 8.5GB，本机 32GB 可直接 `pickle.load`。训练不要把全部历史无窗口地送进 GPU；截面模型按天或按块迭代。

---

## 6. 阶段计划

每个阶段有入口条件、任务清单、交付物、验收标准。不满足验收标准不得进入下一阶段。完成某条后把 `[ ]` 改成 `[x]`，并补一行日期和结果数字。

### 阶段 0 — 合规与环境

状态：**基本完成**（注册与承诺书签字按队伍实际情况自行勾选）

入口：拿到赛题材料。

- [ ] 全员签署《数据资料保密承诺书》并归档（纸质/扫描件放仓库外）
- [ ] 在 <https://satc.jingle.ai/> 注册，确认能打开提交页
- [x] 数据下载到仓库外受控目录，未进 git
- [x] 本地虚拟环境可 import numpy / pandas / scipy / zstandard / lightgbm
- [x] `.gitignore` 覆盖数据包与产物

验收：`git status` 看不到数据文件；读数据脚本能工作。

### 阶段 1 — 数据探查

状态：**已完成**

入口：数据可读。

- [x] 确认键、dtype、形状
- [x] 确认三个 idx 与 train/valid/test 天数
- [x] 确认 mask 比例、按日有效股票数、有标签则有特征
- [x] 确认 `y1` 值域与按日均值，确认测试集标签隐藏
- [x] 确认类别基数
- [x] 扫描 99 列数值特征训练集 RankIC，记录 Top 列
- [x] RankIC 实现与官方描述对齐，训练/验证共用

验收：第 4 节表格与仓库代码一致。本阶段关闭，除非数据文件更新。

### 阶段 2 — 截面快基线（LightGBM）

状态：**未开始** ← 当前应做的下一阶段

入口：阶段 1 关闭。

目标：用**当天**的 `num_x` + 低/中基数 `cat_x`，在验证集上得到可复现的 mean RankIC。不使用历史窗口。这是后续所有模型的对照尺子，也是体验分要求的「先有一条跑得动的流水线」。

#### 必须做

- [ ] `src/dataset.py`：按日展开样本；只保留 `mask_y=True` 的训练点；验证/测试日输出全部 `S` 只股票的分数（无效位置可填中性分）
- [ ] 类别特征：低/中基数进 LightGBM 类别列；`cat_x[..., 5]` 默认先关掉，另开一组对照实验
- [ ] `scripts/train_baseline.py`：只在 train 拟合，只在 valid 报告 RankIC；随机种子写进配置
- [ ] 损失/目标：可先用回归拟合 `y1`，但**验收数字必须是 RankIC**，不是 MSE
- [ ] 预测导出为 `(T_split, S)`，调用 `src.metrics.mean_rank_ic`
- [ ] 记录训练墙钟时间、CPU/内存、特征重要性 Top 20
- [ ] 用同一套权重对 test 推理，走 `save_submission`，本地检查形状 `(442, 5282)`、dtype float32、`np.load` 可无参读取
- [ ] 实验记录写入 `outputs/experiments.md`（gitignore）或仓库内 `experiments.md`（若不含数据与预测值，可入库）

#### 明确不做（本阶段）

- 不上神经网络、不开时序窗口、不做股票图、不调超过 2–3 组树参数（防止把验证集刷穿）
- 不把测试集分数（如果误拿到）写进选模依据

#### 建议超参起点（可改，改了要记）

- `n_estimators` 先 300–800，`learning_rate` 0.05 左右，`num_leaves` 31–63
- 按 `日期` 理解样本相关性：不要用随机 K 折当主验证，主验证永远是官方时间切分
- 若样本太多导致过慢：可按日下采样股票，但最终 valid 评估必须用全市场官方切分

#### 交付物

- 可运行的训练脚本和配置
- 验证集 mean RankIC 数字（写入本文件本节末尾）
- 特征重要性表
- 一份格式正确的 test 预测文件（先不强制上传，上传放阶段 2 末可选）

#### 验收标准（全部满足才关闭）

1. 验证集 mean RankIC **绝对值 > 0.079**（超过最强单因子）。达不到则修数据管道，阶段 2 不关闭
2. 同一配置连续两跑，验证集 RankIC 差异小于 0.01（种子固定）
3. 训练加推理在可接受时间内完成（目标：单卡 CPU 不超过数十分钟量级；超时先减树、减样本，而不是换大模型）
4. 提交文件自检通过
5. 实验记录能让别人用同一配置复现该分数

阶段 2 结果（完成后填写）：

```text
日期：
配置：
valid mean RankIC：
训练耗时：
是否超过单因子 0.079：
提交文件路径（本地）：
备注：
```

决策点：

- valid 已接近或超过 0.12：阶段 4 仍要做（官方提示 + 创新分），但树模型作为融合底仓
- valid 在 0.09–0.12：进入阶段 3 诊断后做时序，优先冲门槛
- valid 低于 0.079：禁止进入阶段 4，先查 mask、是否用了未来信息、是否把无效样本学进去、标签是否对错列

### 阶段 3 — 基线诊断

状态：**未开始**

入口：阶段 2 验收通过。

- [ ] 按日画出 valid RankIC 曲线，标出崩溃日（过低/NaN）
- [ ] 检查崩溃日的有效股票数、特征是否异常、是否全市场同时失效
- [ ] 特征重要性与第 4.5 节单因子方向是否一致（强单因子应出现在重要性前列，符号允许模型自己翻）
- [ ] 分行业/分 `cat_x[..., 6]` 看 RankIC，是否只在某几组股票上有效
- [ ] 记录过拟合迹象：train RankIC 远高于 valid（差距过大要减树深、加正则）

交付物：简短诊断结论（写入实验记录），列出阶段 4 应优先补的点（例如「缺动量类时序」或「某类股票系统性排反」）。

验收：有书面结论，不是只有一个平均分。

### 阶段 4 — 时间维模型

状态：**未开始**

入口：阶段 2、3 关闭。

目标：落实官方提示 1。用 `t` 之前（含缓冲区间 `[0, 486)`）的历史，预测当天分数，验证集超过阶段 2 基线。

- [ ] 明确窗口长度 `L`（先 5/10/20 做三档，不要一上来 200）
- [ ] 窗口构造：第 `t` 天样本只允许使用 `< t` 或规则写明的 `≤ t-1` 特征；当天特征是否纳入要单独消融
- [ ] mask：窗口内缺失要有填充/掩码，不能把 False 当 0 且不告诉模型
- [ ] 模型候选（按实现成本和体验分排序，从前往后试，通过一个再试下一个）：
  1. LightGBM + 历史统计特征（过去 L 日均值/标准差/差值，工作量小，先做）
  2. GRU 或 TCN（中等）
  3. 小规模 Transformer（仅当前两者有效且仍低于门槛时）
- [ ] 主损失考虑排序（pairwise / ListNet / 对截面标准化后的相关损失），MSE 只作辅助
- [ ] 与阶段 2 同表对比：valid RankIC、训练时长、参数量
- [ ] 文档写清硬件和耗时

验收：

1. valid mean RankIC **高于阶段 2**，且不是一次随机波动（至少两个种子或两个相邻窗口设置同向）
2. 训练时长可写入说明书，不出现无法复现的通宵大模型作为唯一方案
3. 有消融：无窗口 vs 有窗口

若时序统计特征已经超过深度模型，主提交可以是「树 + 时序特征」，深度模型降为可选融合支。这不影响创新表述：创新在「时序信息怎么进排序」，不在网络品牌。

### 阶段 5 — 股票维建模与融合

状态：**未开始**

入口：阶段 4 有可复现的时序结果。

目标：落实官方提示 2。

- [ ] 股票维方案（选 1–2 个，不要并行五条）：
  - 当日横截面标准化后再送 MLP/线性
  - 按 `cat_x[..., 6]`（疑似行业）做组内相对强弱
  - 轻量截面注意力（注意 `S≈5000` 的显存，必须采样或分组）
- [ ] 融合：时序分 + 截面分。先做验证集上的线性加权（网格要粗），再考虑学习融合
- [ ] 消融表：仅截面（阶段 2） / 仅时序 / 融合
- [ ] 融合权重只在 valid 上估，估完锁死再打 test

验收：融合 ≥ 最好的单模型；消融表进最终文档。

### 阶段 6 — 提交、文档、体验打磨

状态：**未开始**

入口：阶段 5 选出主方案，或阶段 2/4 已决定先交一版占位（占位提交允许，但主方案仍走完整阶段）。

- [ ] 用锁定配置对 test 出 `(442, 5282)` float32
- [ ] `np.load` 自检；上传 <https://satc.jingle.ai/submit>
- [ ] 记录平台返回分数（只记结果，不把测试集拿来继续海搜超参）
- [ ] 重写 `README.md`：环境、数据路径怎么配、怎么训练、怎么导出、训练耗时、硬件、随机种子
- [ ] 代码整理：入口脚本不超过 3 个常用命令；去掉一次性垃圾文件
- [ ] 说明书里用短段落写创新点：双轴（时间轨迹 + 当日相对位置）+ mask 感知 + 排序指标，对应官方两条提示
- [ ] 确认仓库内无数据、无预测大文件、无密钥

验收：他人按 README 能复现 valid 分数；提交格式被平台接受；文档覆盖设计 20% 与体验 20% 要写的内容。

---

## 7. 实验记录规范

每做一次有效训练，追加一条，禁止只改代码不记数字。

模板：

```text
## 实验 ID
- 日期：
- 代码/配置：
- 输入特征：
- 是否看历史：窗口 = 
- 模型：
- 损失：
- valid mean RankIC：
- train mean RankIC（如有）：
- 耗时 / 硬件：
- 结论（保留 / 抛弃 / 作为融合支）：
- 下一步：
```

对比实验必须控制变量：一次只改窗口、特征集合、损失、模型四者之一。

---

## 8. 实现时的技术要点（写代码对照）

1. **样本粒度**
   - 截面模型：一行 = `(t, stock)`
   - 时序模型：一条 = 某股票在 `t` 的窗口 `(L, Nn[+Nc])`，标签仍是 `y1[t, stock]`
2. **对齐官方切分**：train 拟合、valid 选模、test 只推理一次主方案
3. **mask**：训练损失只用 `mask_y`；推理对全 `S` 出分，避免形状对不上
4. **优化目标**：验收永远是 RankIC。MSE 下降但 RankIC 不动，视为实验失败
5. **类别**：低基数可 embedding 或树模型原生类别；`cat_x[..., 5]` 高基数默认阶段 2 不用
6. **缓冲 486 天**：时序回看合法；截面基线可以不用，但不要把这 486 天的 `y1` 当训练标签（官方 train 起点是 486）
7. **随机性**：种子、线程数写入配置。LightGBM 多线程仍可能有微小差异，以配置和数量级为准
8. **提交占位**：无特征的测试位置填 0 即可，只要 dtype/shape 正确；不要缺股票列

---

## 9. 风险

| 风险 | 后果 | 处理 |
|---|---|---|
| 验证集只有 243 天 | 分数噪声大 | 粗调参；用两个种子确认方向 |
| 门槛 0.12 高于单因子 0.079 | 必须组合/时序 | 阶段 2 先证明组合有效 |
| 8.5GB 内存占用 | 小内存机器训不了 | 分块读、只保留当前窗口；不要再复制一份大数组 |
| 家庭版系统无磁盘加密 | 保密条款要求受控存储 | 数据放 `contest-data-private` 一类仅当前用户可访问的目录，禁止云同步 |
| 深度模型过慢 | 丢体验分 | 树 + 时序统计优先；大网络只作为融合支 |
| 误用 `y2` 或未来窗口 | 本地虚高、提交崩 | 代码审查切分与窗口；PR/改动对照第 3.3 节 |
| 提交 dtype/shape 错误 | 平台拒收 | 只走 `save_submission` |

---

## 10. 近期具体任务（给正在改仓库的人）

按顺序做，不要跳。

1. **保持阶段 1 结论有效**：不要改 RankIC 公式含义；要加速只能优化实现
2. **开始阶段 2**（当前主任务）
   - 骨架已在：`src/dataset.py`、`src/models/baseline.py`、`configs/baseline.yaml`、`scripts/train_baseline.py`
   - 实现 `flatten_masked_rows` 与 `LightGBMBaseline.fit/predict_panel`
   - 训练后把 valid RankIC 填回本文「阶段 2 结果」
   - 导出 test 预测到本地 `submissions/`，不入库
3. 阶段 2 关闭后再开阶段 3 诊断，不要边训边无记录改参
4. `README.md` 等到阶段 6 再写成复现文档；现在以本 `计划.md` 为唯一工作说明

协作时：先改本文件对应 checkbox 和「阶段 2 结果」，再提交代码。没有数字的「大概好了」不算完成。
